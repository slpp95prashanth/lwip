This is a project for learning LwIP Stack from the userspace on linux systems.

-> Build the package by "make"
-> Execute by ./a.out

You would see a shell with capability of few basic networking commands for getting info of the running LwIP stack.
